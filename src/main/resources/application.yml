spring:
  profiles:
    active: local

---
spring:
  config:
    activate:
      on-profile: local
    import: application-local.yml

---
spring:
  config:
    activate:
      on-profile: develop
    import: application-develop.yml